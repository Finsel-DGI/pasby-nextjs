/**
 * @deprecated Prefer `pasbyConfigFromEnv()` from `./runtime-config` or injected `PasbyRuntimeConfig`.
 */
import { pasbyConfigFromEnv } from "./runtime-config";

export default pasbyConfigFromEnv();
